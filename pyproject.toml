[build-system]
requires = ["setuptools>=65.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "wraact"
version = "2026.7.2"
description = "Precise Activation Function Over-Approximation for Neural Network Verification"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}
authors = [
    {name = "Zhongkui Ma", email = "zhongkui@example.com"}
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "pycddlib==3.0.2",
    "numpy==1.26.4",
    "numba==0.59.1",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-cov>=4.0",
    "ruff==0.15.0",
    "mypy>=1.0",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
include = ["wraact*"]
exclude = ["tests*", "docs*"]

[tool.setuptools.package-data]
wraact = ["py.typed"]

[tool.ruff]
line-length = 100
target-version = "py311"
exclude = [
    ".git",
    "__pycache__",
    "build",
    "dist",
    ".venv",
    ".eggs",
]

[tool.ruff.lint]
# Comprehensive rule set (matches propdag standard)
select = [
    "A",     # flake8-builtins
    "B",     # flake8-bugbear
    "BLE",   # flake8-blind-except
    "C4",    # flake8-comprehensions
    "C90",   # mccabe
    "D",     # pydocstyle
    "DOC",   # pydoclint
    "E",     # pycodestyle errors
    "F",     # pyflakes
    "FBT",   # flake8-boolean-trap
    "FURB",  # refurb
    "I",     # isort
    "ICN",   # flake8-import-conventions
    "INP",   # flake8-no-pep420
    "ISC",   # flake8-implicit-str-concat
    "N",     # pep8-naming
    "NPY",   # numpy-specific rules
    "PERF",  # perflint
    "PGH",   # pygrep-hooks
    "PIE",   # flake8-pie
    "PT",    # flake8-pytest-style
    "PTH",   # flake8-use-pathlib
    "Q",     # flake8-quotes
    "RET",   # flake8-return
    "RSE",   # flake8-raise
    "RUF",   # Ruff-specific rules (includes non-ASCII detection)
    "SIM",   # flake8-simplify
    "SLF",   # flake8-self
    "T10",   # flake8-debugger
    "TD",    # flake8-todos
    "TID",   # flake8-tidy-imports
    "UP",    # pyupgrade
    "W",     # pycodestyle warnings
]

ignore = [
    "E501",    # Line too long - ruff format handles this
    "B008",    # Function call in defaults
    "RET504",  # Unnecessary assignment before return
    "SIM108",  # Use ternary operator
    "FBT001",  # Boolean positional arg
    "FBT002",  # Boolean default positional arg
    "D104",    # Missing public package docstring
    "TD002",   # Missing TODO author
    "TD003",   # Missing TODO link
    "T201",    # Print statements - used in debugging/tests
    "PLR0913", # Too many arguments - valid design choice
    "ERA001",  # Commented code - intentional for reference
    "E741",    # Allow ambiguous variable names (mathematical notation: l, O, I)
]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401"]  # Allow unused imports in __init__ (but still detect undefined names from star imports)
"tests/**" = ["INP001", "D100", "D103", "D101", "D102", "C901", "N806", "SLF001"]  # Test code: allow complex functions, mathematical notation, private access
"**/test/**" = ["INP001", "D100", "D103", "D101", "D102", "C901", "N806", "SLF001"]  # Test code: allow complex functions, mathematical notation, private access
"**/tests/**" = ["INP001", "D100", "D103", "D101", "D102", "C901", "N806", "SLF001"]  # Test code: allow complex functions, mathematical notation, private access
"src/wraact/*" = ["N806"]  # Mathematical variable names (Axb = A*x + b)

[tool.ruff.lint.isort]
known-first-party = ["wraact"]

[tool.ruff.lint.flake8-tidy-imports]
ban-relative-imports = "all"

[tool.ruff.lint.mccabe]
max-complexity = 20

[tool.ruff.lint.flake8-quotes]
inline-quotes = "double"
docstring-quotes = "double"

[tool.ruff.lint.pydocstyle]
convention = "pep257"

[tool.mypy]
python_version = "3.11"
mypy_path = "src"
warn_return_any = false
warn_unused_configs = false
warn_unused_ignores = false
disallow_untyped_defs = false
ignore_missing_imports = true
check_untyped_defs = true
no_implicit_optional = false
files = ["src/wraact"]
exclude = ["tests", "venv", ".venv", "build", "dist"]

[tool.coverage.run]
source = ["src/wraact"]
omit = ["*/tests/*", "*/test_*"]

[tool.coverage.paths]
source = [
    "src/wraact",
    "*/site-packages/wraact",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
norecursedirs = ["test_performance"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
timeout = 10
timeout_method = "thread"
addopts = [
    "-v",
    "--tb=short",
    "-ra",
    "--strict-markers",
]

[tool.pyright]
pythonVersion = "3.11"
pythonPlatform = "Linux"
typeCheckingMode = "basic"
include = ["src/wraact"]
exclude = ["tests", "venv", ".venv", "build", "dist"]

# Suppress false positives from bare ndarray (project convention, also used by mypy)
reportMissingTypeArgument = false
reportUnknownParameterType = false
reportUnknownVariableType = false
reportUnknownMemberType = false
reportUnknownArgumentType = false

# numba provides no type stubs
reportMissingTypeStubs = false

# Cascade from unknown ndarray types
reportGeneralTypeIssues = false

# False positives from try/except flow where pyright can't track variable assignment
reportOptionalOperand = false
reportOptionalMemberAccess = false

# False positives from ndarray|float union — isinstance narrowing not tracked by pyright
reportIndexIssue = false

[project.urls]
Homepage = "https://github.com/ZhongkuiMa/wraact"
Repository = "https://github.com/ZhongkuiMa/wraact.git"
Documentation = "https://github.com/ZhongkuiMa/wraact#readme"
